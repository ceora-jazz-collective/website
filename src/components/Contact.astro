---
import { CONTACT_EMAIL } from '../config/variables';
---

<section id="contact" aria-label="Kontakt">
  <div class="section-inner">
    <h2 class="section-title">Kontakt</h2>
    <p class="section-lead">
      Booking, Presse & allgemeine Anfragen — wir freuen uns über Nachrichten.
    </p>

    <div class="contact-grid" role="list" aria-label="Kontaktmöglichkeiten">
      <article class="contact-card" role="listitem" aria-label="Booking">
        <h3>Booking</h3>
        <p class="contact-card__text">
          Für Konzertanfragen am besten per E-Mail — gerne mit Datum, Ort, Setup
          und Rahmenbedingungen.
        </p>
        <p class="contact-card__line">
          <span class="contact-card__label">Name</span>
          <span class="contact-card__value">Uli Hejsek</span>
        </p>
        <p class="contact-card__line">
          <span class="contact-card__label">E-Mail</span>
          <span class="contact-card__value">{CONTACT_EMAIL}</span>
        </p>
        <div class="contact-card__actions">
          <a class="btn btn-primary" href={`mailto:${CONTACT_EMAIL}`}
            >E-MAIL SCHREIBEN</a
          >
        </div>
      </article>

      <article class="contact-card" role="listitem" aria-label="Info">
        <h3>Info</h3>
        <p class="contact-card__text">
          CEORA — Jazz Collective (Wien). Rechtliche Angaben findest du im
          Impressum.
        </p>
        <div class="contact-card__actions">
          <a class="btn btn-secondary" href="/impressum">Impressum</a>
        </div>
      </article>
    </div>
  </div>
</section>
